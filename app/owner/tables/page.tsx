// Owner reuses reception's Table Map — same client component, same auth store,
// so both portals show one implementation instead of two that drift.
export { default } from '@/app/reception/tables/page';
